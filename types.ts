let lastName: string;
let firstName = "Penelope";

let age: number = 34;
let active: boolean = false;

let arrayValues: string[] = ["a", "b", "c"];
let arrayNumbers: Array<number> = [1, 2, 3];

let response: any = "Juan";
response = 45;

type country = "Peru" | "Argentina" | "Chile";
const peru: country = "Peru";

type documentNumber = string | undefined | null;
const dni: documentNumber = "78787878";

function fnPrintMessage(a: number): string {
  return "";
}

const printMessage = (z: string): string => "";
